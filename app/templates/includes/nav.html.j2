 <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{url_for('main.home')}}">POKEMON</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
        {% if current_user.is_authenticated %}
            <li class="nav-item">
            <a class="nav-link" aria-current="page" href="{{url_for('main.home')}}">Home</a>
            </li>
            <li class="nav-item">
            <a class="nav-link" href="{{url_for('main.pokimon')}}">Pokemon</a>
            </li>
             <li class="nav-item">
            <a class="nav-link" href="{{url_for('social.show_users')}}">Battle</a>
            </li>
             <li class="nav-item">
            <a class="nav-link" href="{{url_for('social.show_points')}}">Points</a>
            </li>
            {%else%}
            <li class="nav-item">
            <a class="nav-link" href="{{url_for('auth.register')}}">Register</a>
            </li>
            <li class="nav-item">
            <a class="nav-link" href="{{url_for('auth.login')}}">Login</a>
            </li>
            {%endif%}
        </ul>

        {% if current_user.is_authenticated %}
        <ul class="navbar-nav ms-auto">
            <li class="nav-item">
                 <div class="row">
                <div class="col-12">
                <a class="nav-link" aria-current="page" href="{{url_for('auth.logout')}}">
                Logout
                </a>
                </div>
                <a class="nav-link" aria-current="page" href="{{url_for('auth.edit_profile')}}">
                Edit Profile
                </a>
                </div>
            </li>
        </ul>
        {%endif%}
        </div>
    </div>
    </nav>